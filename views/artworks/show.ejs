<h1><%= artwork.title %></h1>
<p><strong>Artist:</strong> <%= artwork.artist %></p>
<p><strong>Description:</strong> <%= artwork.description %></p>

<% if (artwork.imagePath) { %>
  <img src="<%= artwork.imagePath %>" alt="<%= artwork.title %>" width="300">
<% } %>

<% if (user && artwork.ownerId.equals(user._id)) { %>
  <p>
    <a href="/artworks/<%= artwork._id %>/edit">Uredi</a>
    |
    <form action="/artworks/<%= artwork._id %>?_method=DELETE" method="POST" style="display:inline;">
      <button type="submit">Delete</button>
    </form>
  </p>
<% } %>

<a href="/artworks">Back to Artworks</a>
