<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Artworks | FineArt Gallery</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    /* ===== GLOBAL ===== */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background: #111;
      color: #fff;
      min-height: 100vh;
    }

    /* ===== NAVBAR ===== */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 40px;
      background: #181818;
      border-bottom: 1px solid #333;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .navbar h1 {
      font-size: 1.5rem;
      color: #ffb347;
      letter-spacing: 1px;
    }

    .navbar a,
    .navbar button {
      color: #fff;
      background: none;
      border: none;
      font-size: 1rem;
      text-decoration: none;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .navbar a:hover,
    .navbar button:hover {
      color: #ffb347;
    }

    .navbar .user-info {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .navbar .username {
      font-weight: 600;
      color: #ffb347;
    }

    /* ===== MAIN ===== */
    h2 {
      text-align: center;
      margin: 40px 0 10px;
      font-size: 2.2rem;
      color: #ffb347;
    }

    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 30px;
      padding: 40px;
      max-width: 1300px;
      margin: 0 auto;
    }

    .art-card {
      background: #1d1d1d;
      border-radius: 14px;
      box-shadow: 0 3px 15px rgba(0, 0, 0, 0.5);
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .art-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.6);
    }

    .art-card img {
      width: 100%;
      height: 280px;
      object-fit: cover;
      display: block;
    }

    .art-info {
      padding: 18px;
      text-align: center;
    }

    .art-info h3 {
      color: #fff;
      margin-bottom: 8px;
      font-size: 1.3rem;
    }

    .art-info p {
      color: #ccc;
      font-size: 0.95rem;
      font-style: italic;
      word-wrap: break-word;
      white-space: pre-wrap;
    }

    /* ===== BUTTONS ===== */
  
     .edit-delete {
      position: absolute;
      top: 12px;
      right: 12px;
      display: flex;
      gap: 6px;
      z-index: 10;
    }
    .edit-delete a,
    .edit-delete button {
      background: #ffb347;
      color: #111;
      font-weight: 600;
      border: none;
      padding: 6px 14px;
      border-radius: 8px;
      cursor: pointer;
      text-decoration: none;
      margin: 0 5px;
      transition: 0.3s ease;
    }

    .edit-delete a:hover {
      background: #ffc978;
    }

    .edit-delete button {
      background: #c0392b;
      color: #fff;
    }

    .edit-delete button:hover {
      background: #e74c3c;
    }

    /* ===== ADD ARTWORK BUTTON ===== */
    .add-artwork-btn {
      display: block;
      margin: 40px auto 50px;
      background: #ffb347;
      color: #111;
      font-weight: 600;
      border: none;
      padding: 12px 28px;
      border-radius: 25px;
      text-decoration: none;
      transition: background 0.3s ease;
    }

    .add-artwork-btn:hover {
      background: #ffc978;
    }
  </style>
</head>

<body>

  <!-- ===== NAVBAR ===== -->
  <nav class="navbar">
    <h1>FineArt Gallery</h1>
    <div class="user-info">
      <a href="/">🏠 Home</a>
      <a href="/artworks/new">➕ Add Artwork</a>
      <% if (user) { %>
        <span class="username">👋 Hi, <%= user.username %></span>
        <form method="POST" action="/auth/sign-out" style="display:inline;">
          <button type="submit">🚪 Sign Out</button>
        </form>
      <% } else { %>
        <a href="/auth/sign-in">🔑 Sign In</a>
      <% } %>
    </div>
  </nav>

  <!-- ===== MAIN CONTENT ===== -->
  <main>
    <h2>🎨 My Artworks</h2>

    <% if (artworks.length === 0) { %>
      <p style="text-align:center; margin-top:50px; color:#888;">You haven't uploaded any artworks yet.</p>
    <% } else { %>
      <div class="gallery">
        <% artworks.forEach(artwork => { %>
          <div class="art-card">
            <% if (artwork.imagePath) { %>
              <img src="<%= artwork.imagePath %>" alt="<%= artwork.title %>">
            <% } %>

            <div class="art-info">
              <h3><%= artwork.title %></h3>
              <p><%= artwork.description %></p>
              <p style="color:#ffb347; font-size:0.9rem;">by <%= artwork.artist %></p>
            </div>

            <% if (user && artwork.ownerId.equals(user._id)) { %>
              <div class="edit-delete">
                <a href="/artworks/<%= artwork._id %>/edit">✏️ Edit</a>
                <form method="POST" action="/artworks/<%= artwork._id %>?_method=DELETE" style="display:inline;">
                  <button type="submit">🗑️ Delete</button>
                </form>
              </div>
            <% } %>
          </div>
        <% }) %>
      </div>
    <% } %>

    <a href="/artworks/new" class="add-artwork-btn">➕ Add New Artwork</a>
  </main>

</body>
</html>
